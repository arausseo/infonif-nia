/**
 * Genera el artefacto binario de embeddings del corpus (ADR-004).
 *
 * FASE 2. Todavía no hay corpus: el CNAE-2009 completo (~630 clases) y los ~50
 * casos comerciales curados se incorporan en esa fase. Este script existe ya
 * porque `pnpm build` lo invoca; sale sin escribir nada y lo dice.
 */

console.log(
  "[semantica] Fase 2 pendiente: no hay corpus CNAE ni casos comerciales todavía.\n" +
    "            No se ha generado ningún artefacto de embeddings.",
);
